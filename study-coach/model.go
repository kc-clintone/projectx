package main

import "github.com/kc-clintone/study-coach/model"

// Type aliases to keep existing code compiling while using model definitions.
type Task = model.Task
type Session = model.Session
type CreateSessionRequest = model.CreateSessionRequest
type Submission = model.Submission
type StudyPlan = model.StudyPlan
type TopicSnapshot = model.TopicSnapshot
type Badge = model.Badge
type ProfileResponse = model.ProfileResponse
type TopicImprovement = model.TopicImprovement
